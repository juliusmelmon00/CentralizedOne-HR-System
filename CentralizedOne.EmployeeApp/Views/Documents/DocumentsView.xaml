<UserControl x:Class="CentralizedOne.EmployeeApp.Views.Documents.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:helpers="clr-namespace:CentralizedOne.EmployeeApp.Helpers"
             mc:Ignorable="d"
             Background="White">

    <!-- ✅ Register Converters (add BooleanInverseConverter too if not already in Converters.xaml) -->
    <UserControl.Resources>
        <helpers:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <helpers:BooleanInverseConverter x:Key="BooleanInverseConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Upload Button -->
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="📂 My Documents" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Foreground="#1c2c3c"
                       VerticalAlignment="Center"
                       Margin="0 0 15 0"/>

            <!-- ✅ MVVM-bound Upload Button -->
            <Button Width="160"
                    Height="32"
                    Command="{Binding UploadDocumentCommand}"
                    IsEnabled="{Binding IsUploading, Converter={StaticResource BooleanInverseConverter}}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Content" Value="📤 Upload Document"/>
                        <Setter Property="Background" Value="#1c2c3c"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsUploading}" Value="True">
                                <Setter Property="Content" Value="⏳ Uploading..."/>
                                <Setter Property="Background" Value="#7a869a"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Status Label -->
        <TextBlock x:Name="LblStatus"
                   Text="Loading documents..."
                   Grid.Row="1"
                   FontSize="14"
                   Foreground="#7a869a"
                   Visibility="Collapsed"
                   Margin="5"/>

        <!-- Documents List -->
        <ListView x:Name="ListDocuments" Grid.Row="1" Margin="0,30,0,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}" />

                    <!-- ✅ Styled Status Pill -->
                    <GridViewColumn Header="Status" Width="140">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="6 2"
                                        CornerRadius="12"
                                        HorizontalAlignment="Center"
                                        Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}">
                                    <TextBlock Text="{Binding Status}" 
                                               Foreground="White"
                                               FontWeight="Bold"
                                               FontSize="12"
                                               TextAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Rejection Reason" Width="250" DisplayMemberBinding="{Binding RejectionReason}" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
