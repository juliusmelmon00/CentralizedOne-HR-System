<Window x:Class="CentralizedOne.EmployeeApp.Views.Documents.ExpiryDateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set Expiry Date"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        Width="350" Height="200"
        Background="White">

    <Border CornerRadius="8" Padding="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Select Expiry Date"
                       FontSize="16"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0 0 0 10"/>

            <!-- Just DatePicker, default date handled in code-behind -->
            <DatePicker x:Name="DateInput"
                        Margin="0 0 0 15" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="✅ Confirm"
                        Width="100"
                        Margin="0 0 10 0"
                        Click="Confirm_Click"/>
                <Button Content="❌ Cancel"
                        Width="100"
                        Click="Cancel_Click"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
